# EDTfind

Библиотека OneScript для поиска каталога исполняемого файла EDT (1C:Enterprise Development Tools).

## Назначение

Модуль `EDTfind` **надёжно определяет**, чем запускать EDT-операции, и возвращает каталог, в котором расположен исполняемый файл EDT:

- Для EDT **2024+**: `1cedtcli` (`1cedtcli.exe` на Windows)
- Для EDT **до 2024**: `ring` (`ring.cmd` на Windows)

При ошибках модуль выдаёт диагностику с указанием каталогов поиска и доступных версий.

## Поддерживаемые ОС

- Windows
- Linux

## Установка

```bash
opm install edtfind
```

## Использование

### Как библиотека (API)

```bsl
#Использовать edtfind

Поиск = Новый ПоискEDT();

// Найти максимальную установленную версию EDT
КаталогEDT = Поиск.НайтиКаталогИсполняемогоФайла();

// Найти конкретную версию EDT
КаталогEDT = Поиск.НайтиКаталогИсполняемогоФайла("2024.1.2");
```

### Как CLI-приложение

```bash
# Найти максимальную установленную версию
edtfind find

# Найти конкретную версию
edtfind find --edt-version 2024.1.2

# Подробный вывод (отладка)
edtfind find -v
```

## Алгоритм работы

1. **Сбор кандидатов** — сканирование стандартных каталогов установки EDT по файлам-маркерам (`1cedt.ini`, `1cedt`).
2. **Извлечение версий** — из имён каталогов установки извлекается номер версии (например, `1c_edt_2024.1.2` → `2024.1.2`).
3. **Выбор версии** — если версия задана явно, выбирается она; иначе — максимальная найденная (semver по убыванию).
4. **Определение исполняемого файла** — для 2024+ ищется `1cedtcli`, для старых версий — `ring`.

### Каталоги поиска по умолчанию

#### Windows

- `%LOCALAPPDATA%\1C\1cedtstart\installations`
- `C:\Program Files\1C\1CE\components`

#### Linux

- `~/.local/share/1C/1cedtstart/installations`
- `/opt/1C/1CE/components`

## Диагностика ошибок

При невозможности найти EDT модуль выбрасывает исключение с сообщением:

- **Не найдено ни одной установленной версии EDT** — с указанием проверенных каталогов.
- **Запрошенная версия EDT не найдена** — с перечислением доступных версий.
- **Версия 2024+, но `1cedtcli` не найден** — проверьте целостность установки EDT.
- **`ring` не найден** — проверьте установку EDT/компонентов 1C.

## Зависимости

- [logos](https://github.com/oscript-library/logos) — логирование
- [cli](https://github.com/oscript-library/cli) — парсинг аргументов командной строки
- [fs](https://github.com/oscript-library/fs) — работа с файловой системой

## Лицензия

[MPL-2.0](LICENSE)
